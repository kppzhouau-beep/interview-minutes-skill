---
name: interview-minutes
description: 将专家、客户、管理层或调研访谈的语音转写稿整理为机构级中文 Word 纪要；适用于需要术语纠错、企业名称核验、内容溯源、企业模板零偏差复刻以及内容与版式双重审核的任务。
---

# Interview Minutes — 机构级访谈纪要

把带有说话人标签、时间戳和转写错误的访谈稿，整理成可用于研究、尽调或内部汇报的 Word 纪要。输出必须同时满足内容可追溯和企业模板零偏差，不得以通用文档美化规则覆盖本 Skill 的模板。

本 Skill 必须以完整目录安装和调用，不能只复制 `SKILL.md`。若 `assets/template_minutes.docx`、`assets/build_minutes.py`、`assets/template_manifest.json` 或 `scripts/verify_docx_format.py` 任一缺失，立即停止并提示安装不完整，不得降级为通用 LLM 自行生成 Word。

## 不可绕过的交付门槛

以下要求直接决定文件能否交付：

1. 没有用户指定模板时，必须以 `assets/template_minutes.docx` 为母版；不得使用 `Document()` 从零创建 Word。
2. 必须使用 `assets/build_minutes.py` 生成成稿。允许修改内容 JSON，不允许另写带有全局字体设置、样式归一化或重新编号的生成脚本。
3. 不得调用 `set_run_font`、`font.name`、`w:rFonts` 批量覆盖、样式规范化或其他通用“美化”逻辑。标题黑体、正文楷体、辅助西文字体和粗体范围全部从模板 OOXML 克隆。
4. 必须保留模板的 `styles.xml`、`numbering.xml`、`settings.xml` 和主题文件；默认模板还必须通过 `assets/template_manifest.json` 指纹核验。
5. 必须使用模板自动编号。写入段落的文字不得再带“一、”“1.”“➢”“-”等手写编号或项目符号。
6. 生成脚本必须输出 `FORMAT CHECK PASSED` 后才可交付；出现 `BUILD FAILED` 或 `FORMAT CHECK FAILED` 时必须修正，不得绕过校验或直接交付失败文件。
7. 生成后仍须渲染为 PDF／逐页图片检查。结构校验通过不能替代视觉审核。

## 标准标题与文件名

- 文档标题：`{项目名称}-{访谈对象}{访谈类型}`
- 文件名：`{项目名称}_{访谈对象}{访谈类型}纪要_{YYYYMMDD}.docx`
- 示例标题：`示例项目-某机构专家访谈`
- 示例文件名：`示例项目_某机构专家访谈纪要_20260911.docx`

项目名称、访谈对象、访谈类型和日期不得由模型猜测。优先使用用户明确提供的信息，其次使用原始文件名或同目录已确认材料。仍无法确定且会影响标题或文件名时，必须向用户确认。

不得在生成后随意重命名文件；文件名属于强制格式校验的一部分。

## 标准工作流程

### 1. 建立内部工作清单

至少跟踪：参考模板、原始转录、术语与名称核验、内容重构、Word 成稿、内容审核、版式审核。运行环境支持任务工具时使用任务工具，否则维护内部底稿。

### 2. 读取模板与原始转录

- 用户指定参考文档时，该文档是唯一格式权威，并作为 `--reference` 传给生成脚本。
- 未指定时使用随包企业模板 `assets/template_minutes.docx`。
- 开始生成前必须完整阅读 [模板与 OOXML 协议](references/format_protocol.md)。
- A 型转录可用 `python-docx` 遍历段落；WPS 将全文塞入单段并以 `<w:br/>` 分隔时，必须直接解析 `word/document.xml`。
- 长录音包含多场访谈时，先按时间轴、称呼、履历、职责、入场／致谢和话题切换建立内部“分段判断与证据表”，再分别成稿；预定日程只能作辅助。

### 3. 术语校对与名称核验

- 先读取 `assets/glossary_pcb_ldi.md`；若行业不同，为本次任务建立临时术语表。
- 重点检查公司／机构名、技术术语、英文缩写、数字量级和单位。
- 计划写入正文的全部主体名称必须按 [名称核验协议](references/name_verification.md) 执行分类双重核验。
- 国内企业优先使用企查查 MCP 加官网或权威来源；企查查仅用于主体识别，不得把外部业务信息补进纪要。
- 名称无法唯一确认时省略或泛化，不得猜测。

### 4. 重构内容

- “主要结论”和正文只能取材于本次原始转录；用户明确授权的补充材料须单独记录来源。
- 每项事实、数字、判断和因果关系必须能回指原始转录。
- 不补全未说出的前提，不把含糊表述改成确定结论，不混合不同主体、业务、时间或统计口径。
- 删除工作餐、等待、接送、个人生活、股票及其他无关闲聊；独立事实、限定条件、阶段状态、风险和否定性信息不得因追求简短而遗漏。
- 使用直接、客观的表达，避免“专家认为”“专家估计”“按专家粗算”“专家称”等机械归因。
- 正式纪要不得出现录音边界、时间戳、转写质量、核验过程或制作说明，例如“录音完整性”“口算更正”“名称处理”“待核验”“未能确认”。

### 5. 准备内容 JSON

按以下结构制作 UTF-8 JSON。元信息无可靠信息时可以留空，但不得使用 `111`、`XXX`、`TBD`、“待补充”等占位值。

```json
{
  "project": "示例项目",
  "subject": "某机构",
  "interview_type": "专家访谈",
  "date_compact": "20260911",
  "meta": {
    "访谈时间": "2026年9月11日",
    "访谈地点": "线上",
    "访谈来源": "公司",
    "访谈对象及其背景介绍": "姓名、职务及相关背景",
    "访谈人员": "张三、李四"
  },
  "conclusions": [
    {"lead": "核心结论：", "body": "结论正文。"}
  ],
  "sections": [
    {"kind": "section", "text": "一级章节标题"},
    {"kind": "subsection", "text": "二级标题"},
    {"kind": "point", "lead": "要点引导语：", "body": "要点正文。"}
  ]
}
```

`section`、`subsection` 和 `point` 的文本中不得手写编号。引导语和正文必须拆分为 `lead` 与 `body`，以保留模板规定的粗体范围。

### 6. 用强制生成器成稿

```bash
python3 assets/build_minutes.py \
  --payload /absolute/path/payload.json \
  --output-dir /absolute/path/output
```

若用户指定参考模板：

```bash
python3 assets/build_minutes.py \
  --payload /absolute/path/payload.json \
  --reference /absolute/path/reference.docx \
  --output-dir /absolute/path/output
```

生成器会自动确定标题和文件名，并调用 `scripts/verify_docx_format.py`。不要在生成后重新设置字体、套用主题、重新编号或另存为其他名称。

### 7. 内容与版式双重独立审核

完整执行 `assets/review_checklist.md`：

1. 逐条将主要结论和正文事实回指原始转录。
2. 枚举未进入正文的原文条目，判断为重复、提问、串话、闲聊、无法归属或需补回。
3. 检查主体、时间、数字、单位、产品和技术路线，排除混淆、确定性拔高及前后矛盾。
4. 重新提取全部公司／机构名称，与名称核验表逐项勾稽。
5. 扫描禁用制作说明和机械归因词。
6. 再次运行格式校验器；用户指定模板时必须传同一 `--reference`。
7. 使用同一渲染配置转换为 PDF 和逐页图片，逐页检查标题、元信息表、字体、编号、缩进、分页、空白页、孤行和异常留白。

任何修改都必须重新执行格式校验和逐页渲染。

## 局部修订既有纪要

- 先复制原件，再局部替换或补充；未涉及的内容、样式、编号、分页和 OOXML 属性保持不变。
- 新增段落只能克隆同层级原段落，不得通过默认样式或全局字体函数创建。
- 修订后运行格式校验器，并以原件或用户指定参考文档作为 `--reference`。

## 交付要求

- 只交付通过内容审核、`FORMAT CHECK PASSED` 和逐页视觉审核的最终 `.docx`。
- 简要说明关键术语、名称或事实修正；正式纪要不附内部核验过程。
- 如果格式校验仍失败，明确报告失败，不得把近似格式包装成已完成结果。
