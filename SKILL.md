---
name: interview-minutes
description: 将专家、客户、管理层或调研访谈的语音转写稿整理为机构级中文 Word 纪要；适用于需要术语纠错、企业名称核验、内容溯源、参考模板零偏差复刻以及内容与版式双重审核的任务。
---

# Interview Minutes — 机构级访谈纪要

把带有说话人标签、时间戳和转写错误的访谈稿，整理成可用于研究、尽调或内部汇报的 Word 纪要。

## 核心结果

- 输出包含标题、元信息、主要结论和分章节访谈纪要。
- 每项事实、数字、判断与因果关系均能回指本次原始转录。
- 纠正行业术语和音近公司名，但不把外部资料的业务信息写进纪要。
- 严格复用用户指定模板的 OOXML 样式；没有指定模板时使用 `assets/template_minutes.docx`。
- 完成内容与版式两轮独立审核后再交付。

## 工作流程

### 1. 建立工作清单

跟踪以下五项：参考模板、原始转录、术语与名称核验、Word 成稿、内容与版式审核。运行环境支持任务管理工具时使用该工具，否则在内部工作底稿中维护。

### 2. 读取参考模板

- 用户指定参考文档时，以该文档为唯一格式权威。
- 没有指定时，使用 `assets/template_minutes.docx`。
- 读取 `document.xml`、`styles.xml`、`numbering.xml`、节属性和元信息表，记录标题、字体、字号、间距、缩进、编号及分页方式。
- 需要复制模板或生成 Word 时，先阅读 [模板与 OOXML 协议](references/format_protocol.md)。

### 3. 解析原始转录

先判断文档结构：

- A 型：每段为独立 paragraph，可用 `python-docx` 遍历。
- B 型：WPS 常将整篇内容放在一个 paragraph 中，以 `<w:br/>` 分隔；必须直接解析 `word/document.xml`。

```python
import re
import zipfile

with zipfile.ZipFile(input_docx) as archive:
    xml = archive.read("word/document.xml").decode("utf-8")
xml = re.sub(r"<w:br\s*/>", "\n", xml)
texts = re.findall(r"<w:t[^>]*>(.*?)</w:t>", xml, re.DOTALL)
full_text = "\n".join(texts)
```

如果常规读取只有少数几行但文件明显较大，立即切换到 B 型解析。长录音包含多场访谈时，先按时间轴、称呼、履历、职责、入场／致谢和话题切换建立内部“分段判断与证据表”，再分别成稿；预定日程只能作辅助线索。

### 4. 术语校对与名称核验

- 先阅读 `assets/glossary_pcb_ldi.md`；不属于该行业时，为本次任务建立临时术语表。
- 重点检查公司名、机构名、技术术语、英文缩写、数字量级及单位。
- 吃不准的名称省略或泛化，不凭行业常识猜测。
- 计划写入正文的全部主体名称必须按 [企业与机构名称核验协议](references/name_verification.md) 核验。
- 外部资料仅用于纠正名称，不得把检索得到的业务、客户、收入、融资或判断补进纪要。

### 5. 重构内容

遵守以下强制边界：

- “主要结论”和正文只能取材于本次原始转录；用户明确授权的补充书面材料需单独记录来源。
- 不补全说话人未说出的前提，不把含糊表述改成确定结论，不混合不同主体、业务、时间或统计口径。
- 数字或算术冲突只进入内部核验底稿。能够确认时只写最终口径；无法确认且不影响主旨时省略数字。
- 删除工作餐、等待、接送、个人生活、股票及其他与本次研究无实质关系的闲聊。
- 保持信息完整：独立事实、限定条件、阶段状态、风险和否定性信息不得因追求简短而遗漏。
- 使用直接、客观的陈述，不反复使用“专家认为”“专家估计”“按专家粗算”“专家称”等机械归因。
- 正式纪要不得出现录音边界、时间戳、转写质量、核验过程或制作说明，例如“录音完整性”“覆盖范围”“口算更正”“名称处理”“待核验”“未能确认”。无法确认的细节在内部底稿处理。

### 6. 生成或局部修订 Word

- 新建纪要时，以参考模板或 `assets/template_minutes.docx` 为母版，优先使用 `assets/build_minutes.py`。
- 只替换文本并深拷贝模板段落的 `w:pPr`、`w:rPr`、style、`numId` 和 `ilvl`；不得从零近似重建格式。
- 元信息表保持原字段和样式。“访谈对象及其背景介绍：”与对象信息必须同段连续展示。
- 主要结论结束后受控分页，使“访谈纪要”从新页页首开始；以最终 PDF 实测为准，避免空白页。
- 用户要求修改既有纪要时，先复制原件，再局部替换或补充。未涉及的内容、样式、编号、分页和 OOXML 属性保持不变，并生成段落级差异清单。

### 7. 内容与版式双重独立审核

完整执行 `assets/review_checklist.md`，至少包括：

1. 逐条将主要结论和正文事实回指原始转录。
2. 枚举未进入正文的原文条目，归类为重复、提问、串话、闲聊、无法归属或需补回。
3. 检查主体、时间、数字、单位、产品和技术路线，排除混淆、确定性拔高及前后矛盾。
4. 重新提取成稿中的全部公司／机构名称，与名称核验表逐项勾稽。
5. 扫描禁用制作说明和机械归因词。
6. 程序化比较参考文档与成稿的页面设置、表格、样式、字体、字号、粗体、间距、缩进及编号。
7. 使用同一渲染配置把参考文档和成稿转换为 PDF，并逐页检查首页、分页、末页、孤行、空白页和异常留白。

审查发现问题后修正并重新执行两轮审核，直至通过。

## 交付

- 交付最终 `.docx`，并提供可点击的本地文件路径。
- 简要说明最关键的术语、名称或事实修正。
- 正式纪要不附内部核验过程；必要时可另行交付名称核验表或来源溯源表。

## 禁止事项

- 不凭印象重新生成内容或添加行业常识。
- 不把不同访谈、说话人、产品、地区、时间或统计口径合并。
- 不把无法确认的公司名硬猜成真实主体。
- 不把录音边界、转写不清、待核、未量化等后台质控信息写入正式纪要。
- 不为压缩篇幅遗漏独立信息。
- 不为适配本机渲染而修改 Word 中的规范字体名称。
- 不省略内容审核、名称核验或逐页版式检查。
